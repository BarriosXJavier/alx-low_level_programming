#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
int i;
/* Loop from 0 to 9 */
for (i = 0; i < 10; i++)
{
      /** Print the number followed by a new line */
printf("%d\n", i);
}
return (0);
}

