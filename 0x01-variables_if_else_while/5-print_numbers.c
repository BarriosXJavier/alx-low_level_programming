#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
int i;
/* Loop from 0 to 9 */
for (i = 0; i < 10; i++)
{
      /** Print the number */
printf("%d", i);
}
return (0);
}

