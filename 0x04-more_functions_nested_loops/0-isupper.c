#include "main.h"
/**
 * _isupper - Check whether a letter is upper
 * @c: the number under check
 * Return: 1 for upper letter or 0 if not
 */
int _isupper(int c)

{
	if (c >= 65 && <= 90)
	{
	return (1);
	}
	return (0);
}
