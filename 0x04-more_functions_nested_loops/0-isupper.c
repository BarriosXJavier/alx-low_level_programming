#include "main.h"
/**
 * _isupper - Check whether a letter is upper
 * @x: the number under check
 * Return: 1 for upper letter or 0 if not
 */
int _isupper(int x)
{
	if (x >= 65 && <= 90)
	{
	return (1);
	}
	return (0);
}
