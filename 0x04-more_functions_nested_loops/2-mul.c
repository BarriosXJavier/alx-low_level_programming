#include "main.h"
#include <stdio.h>

/**
 * mul - multiplying two integers
 * @a - 1st integer
 * @b - 2nd integer
 * Return: The result of the two  integers
 */
int mul(int a, int b)

{
	int result = a * b;

	return (result);
}
