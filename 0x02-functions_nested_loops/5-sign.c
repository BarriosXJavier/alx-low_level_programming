#include "main.h"

/**
 * 5-print_sign - prints a number's sign
 * Return: 1 , prints + if r is greater than 0, - if r < 0
 */
int print sign(int r)
{
	if (r > 0)
	{
		_putchar('+');
		return (1);
	}else if (r==0)
}
